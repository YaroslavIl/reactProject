import React from 'react';
import { Link, Outlet, Route } from 'react-router-dom'
import UserInfo from '../UserInfo';

const Layout = () => {
    return (
        <div>
            
        </div>
    );
};

export  {Layout};